# HashLogin

Aplicação Java simples para autenticação usando **BCrypt** e **H2 Database** via **JDBC**.

### Para acessar o console do H2

Após iniciar a aplicação, acesse:
```
http://localhost:8082
```

A aplicação utiliza Maven como gerenciador de pacotes.
